
To create an origin with the bio CLI, use the [`bio origin create`]({{< relref "biome_cli.md#bio-origin-create" >}}) command. For example:

- ```bash
  bio origin create <ORIGIN>
  ```

  Biome creates an origin on the Biome Builder site.
