version: '3.8'
services:
  tester:
    image: ${TESTCASE}_image
    domainname: biome.dev
    environment:
      BIO_AUTH_TOKEN: ${BIO_AUTH_TOKEN}
      BIO_LICENSE: accept-no-persist
      BIO_NONINTERACTIVE: '1'
      TESTCASE: ${TESTCASE}
      COMPOSE_PROJECT_NAME: ${COMPOSE_PROJECT_NAME}
    volumes:
      - source: ./CTL_SECRET
        target: /bio/sup/default/CTL_SECRET
        type: bind
      - source: /var/run/docker.sock
        target: /var/run/docker.sock
        type: bind
      - source: ../../../.expeditor/scripts
        target: /scripts
        type: bind
    build:
      context: ${TESTCASE_DIR}
    depends_on:
      - bastion
      - alpha
      - beta
      - gamma

  alpha:
    environment:
      BIO_UPDATE_STRATEGY_PERIOD_MS: 3000
      BIO_UPDATE_STRATEGY_PERIOD_BYPASS_CHECK: 1

  beta:
    environment:
      BIO_UPDATE_STRATEGY_PERIOD_MS: 3000
      BIO_UPDATE_STRATEGY_PERIOD_BYPASS_CHECK: 1

  gamma:
    image: ${SUPERVISOR_IMAGE}
    domainname: biome.dev
    environment:
      BIO_AUTH_TOKEN: ${BIO_AUTH_TOKEN}
      BIO_LICENSE: accept-no-persist
      BIO_NONINTERACTIVE: '1'
      TESTCASE: ${TESTCASE}
      COMPOSE_PROJECT_NAME: ${COMPOSE_PROJECT_NAME}
      BIO_UPDATE_STRATEGY_PERIOD_MS: 3000
      BIO_UPDATE_STRATEGY_PERIOD_BYPASS_CHECK: 1
    volumes:
      - source: ./CTL_SECRET
        target: /bio/sup/default/CTL_SECRET
        type: bind
    command:
      - run
      - --listen-ctl=0.0.0.0:9632
      - --peer=bastion.biome.dev
    hostname: gamma
    networks:
      default:
        aliases:
        - gamma.biome.dev
    depends_on:
      - bastion
